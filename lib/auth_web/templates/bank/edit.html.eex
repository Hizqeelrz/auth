<h2>Edit Bank Location</h2>

<%= render "form.html", Map.put(assigns, :action, bank_path(@conn, :update, @bank)) %>

<span><%= link "Back", to: bank_path(@conn, :index), class: "btn btn-info btn-sm" %></span>
